# 设计一致性修订基线

> 状态：**已批准，分步实施**  
> 范围：跨 S01/S03/S04/S08/S09/S10/S15/S21 的规则治理，不新增并列大系统。  
> 原则：本文记录目标规则与迁移顺序；在代码、数据、UI、API 和验证全部完成前，不得把对应项标为“已实装”。

## 一、为什么修订

全量文档审查确认：游戏最有价值的主循环是“情报判断 → 任命与编成 → 郡域节点和补给 →
战斗模式选择 → 伤亡、人口、忠诚与外交后果”。当前主要风险不是缺系统，而是时间单位、
公式量纲、战斗层级、单挑交互、成长入口和文档摘要之间存在冲突，导致规则无法稳定平衡。

本轮冻结继续横向增加系统，先按下列 P0～P4 修复。每次只实施一个大系统或一组最小依赖，
遵守 `AGENTS.md` 的验证与文档双写规则。

## 二、批准后的统一规则

### P0 — 口径和真源

1. **时间真源**：`1 回合 = 1 个月`，`3 回合 = 1 季`，`12 回合 = 1 年`。
   “每月”是推进频率；“季初/季末”是第 1/4/7/10 月触发的季度结算，不再把“季”当作回合别名。
2. **战争结构**：统一称为“战争四层、三种交战结算模式”。
   四层是行政大地图 → 郡域战场 → 局部交战 → 单挑演出；三种结算模式是自动、标准、六角微操。
   白刃表现属于标准模式的表现层，不再与六角并列成第二套必经结算。
3. **规模数字**：阵型全量为 27（18 陆 + 9 水）；隐藏属性为 19（11 + 8）。
4. **原创表述**：允许并鼓励本项目原创程序化表现；禁止的是把虚构具名人物冒充史实，以及无授权仿制
   商业作品。历史剧本、演义层、假想剧本与纯玩法改编必须显示来源层级。

### P1 — 公式和契约

1. 所有概率公式统一以**百分点**书写，最终一次 `clamp` 到 `[5%, 90%]`；禁止小数概率和百分点混算。
2. 登用率目标公式：

   ```text
   40
   + (说客魅力 - 目标魅力) × 0.30
   + (1 - |相性差| / 150) × 40
   + 目标义理 × 2
   - 目标野心 × 3
   + 技能/关系/处境修正
   ```

3. 外交成功率目标公式：

   ```text
   35
   + 双边友好度 × 0.35
   + (我方声望 - 对方声望) / 100
   + 使者魅力 × 0.15
   + 正当理由/共同敌人/条约修正
   - 对方戒备与利益冲突
   ```

   双边友好度只能出现一次，不得同时“加友好”又“减目标友好”。
4. 同盟不再硬锁 10 年：基础期限 24 个月，期内可毁约但承担信誉、友好和宣战理由后果；
   联姻不硬锁 15 年，姻亲关系持续存在，60 个月内毁约追加更重惩罚。停战和互不侵犯使用明确月数。

### P2 — 决策密度和战斗公平

1. 单挑采用“**一次战略介入 + 自动演算**”：玩家在发起/应战时选择
   `强攻 / 持重 / 诱敌 / 委任` 之一，仅改变自动指令权重和风险，不逐回合点牌。
   AI 与跳过演出均走同一权威结算。文案不得把玩家不可干预的内部随机称作玩家“心理博弈”。
2. 吕布保持全游戏最强单挑者，但不拥有数学上的绝对胜利：
   - 所有必杀仍服从核心克制，只给吕布抗克制和伤害优势；
   - 吕布可被击败；传奇保护只把首次致死/被俘结果改为重伤撤退，不篡改胜负；
   - 禁止“不可化解 + 被动高概率化解 + 失败减伤 + 不死不俘”四重叠加。
3. 成长界面收敛为四个玩家可读入口：人物成长、军团战备、阵型精通、战役态势。
   其余底层数值作为解释这些入口的细项，不再同时抢占主 UI 和奖励反馈。

### P3 — 经济、内政与 AI

1. 持续型内政是目标规则；当前即时开发明确标为 Demo 兼容切片。迁移时必须提供项目队列、
   剩余月数、中断损失和季度收益预览。
2. 建立标准化年度预算表：以单城、三城和十城三个样本核对金粮收入、军费、俸禄、
   建设回收期、战争损失和行政成本；没有预算表不得继续增加资源种类或常驻消耗。
3. AI 从“每势力一支进攻军”迁移到受资源、将领和前线数量约束的多线计划；
   难度优先提高信息处理、编成和目标选择，经济修正设软上限，不再给武将五维永久 `+5`。
4. 增加反滚雪球支点：跨城行政成本、占领整合期、补给线压力与弱势外交结盟价值。

### P4 — S09 语义迁移

S09 目标名称改为“**宫廷人脉**”（Session 248 已迁为
`courtNetwork/courtNetworkOpportunities`；旧字段仅由 v1 加载迁移器读取）。
资源代表可用于赏赐、联姻牵线、谍报掩护和宫廷交涉的人脉机会，不代表人口中的女性数量：

- 删除“约 400 成年女性 = 1 点”“寻访扣成年女性”的规则；
- 来源改为市井声望、豪族往来、宴饮、战后接管与事件；
- 历史女角仍只通过史实事件、亲属跟随、联姻和剧本配置出现；
- 迁移完成前，UI 必须避免把库存描述成可买卖或抢夺的“女性实体”。

## 三、实施顺序与验收

| 顺序 | 主系统 | 实施内容 | 最低验收 |
|:--:|------|---------|---------|
| **R1 ✅** | S01 | 月回合与季度触发统一 | 连续推进 12 回合：月 12 次、季 4 次、年 1 次（28/28） |
| **R2 ✅** | S11/S08 | 登用与外交公式单位修正 | 边界、单调性、固定 seed 与 UI 百分比一致（20/20） |
| **R3 ✅** | S10 | 单挑倾向 + 吕布规则 | 四倾向可选；同 seed 可复现；吕布强但存在可验证败局 |
| **R4 ✅** | S21 | 四层/三模式命名和状态映射 | 自动、标准、六角三入口只结算一次并正确回写 |
| **R5 ✅** | S03/S04 | 持续内政与年度预算 | 三类样本城 12 月预算可解释，无免费循环 |
| **R6 ✅** | S15 | 多线 AI 与公平难度 | AI 可守、可撤、可双线；无五维作弊；固定 seed 复现（38/38） |

> **R6 范围边界（Q12 已声明，Session 174）**：县级攻打决策（攻占
> `BattlefieldInstance` 内属县节点）**不属于 BF-P2/P3 范围**，归入 R6
> 多线 AI 能力。当前 BF-P3（AI 决策整场复现）仅覆盖出征/围城/自动战斗层面的
> 既有决策 RNG 收口，**不含县级战术目标选择**。BF-P2 引入的"玩家可攻打县 +
> AI 守县（被动响应，沿用既有 `runAutoBattle`）"不属于新 AI 决策类型；AI 主动
> 攻县属 R6 范畴，与 BF-P3 解耦。详见 `docs/25-bf-p2-design.md` §五。
>
> **R6 待办依赖补登记（Session 177，2026-07-24，老实标注；Session 254 更新）**：
> BF-P2 Q9 落地时发现两项前置依赖必须由 R6（或 BF-P5）承接：
> 1. **补给线糧耗×2 真实路径判定 —— ✅ 已解决（BF-P5，Session 254）**：
>    BF-P2 原用"占领任意首批县→守方全部 Army morale -5"全局简化替代
>    （见 `docs/25-bf-p2-design.md` §2.6.1）。BF-P5 已通过
>    `shared/army-county-mapping.ts` 建立 `CampaignArmy`（数字 cityId）与
>    郡域县节点（字符串 countyId）的**位置映射**（`nodeStates[].armyIds` 权威 +
>    deployments 回退），`tickBattlefieldInstance` 逐军真实路径判定（补给线 =
>    seat → Army 当前县最短路径，经过攻方控制县 → 粮耗×2 + 士气-5），全局简化
>    已下线。**R6 剩余部分 —— ✅ 已解决（Session 258，2026-08-01）**：守方 Army
>    在郡域场景内的 countyId 定位（多线 AI 行为前置）——`enterNanjunBattlefield`
>    守方势力改为郡治大地图城市（worldCityId）实际占领势力，驻留郡治城市的守方
>    现役 Army（`currentNodeId === worldCityId`）自动纳入郡域战场，部署到模板
>    `defenderEntryNodeIds`（守方纵深前沿县）；补给线真实路径判定在真实流程触发
>    （f9 真实流程，`verify-save-battlefield-instance.ts` 74/74）。
> 2. **郡域场景迷雾机制 —— ✅ 已解决（BF-P5，Session 256）**：BF-P2 Q9 的
>    "视野扩张"攻占效果原未实现（郡域场景无迷雾遮蔽，只有占领视觉反馈，
>    见 `docs/25-bf-p2-design.md` §2.6.2）。BF-P5 已把迷雾层下沉到
>    `BattlefieldInstance`：`shared/commandery-fog.ts` 纯函数（`computeRevealedNodeIds`
>    + `maskBattlefieldInstanceForPlayer`）+ `maskGameStateForPlayer` 集成；
>    `foggedNodeIds` 为 mask 投影专属字段（Zod optional，**不入存档**）。
>    地理层恒可见、军情层按揭示集遮蔽；揭示集 = 入口县 ∪ 郡治 ∪ 攻方 Army
>    所在县 ∪ 攻方已占领县（每源 + 一跳邻接）。**占领县→成为揭示源→邻接县破雾**
>    = 视野扩张攻占效果完整实现。验证：fog 单测 8/8、`verify-save-battlefield-instance.ts`
>    f8（63/63）、真实 API + Headless Chrome。**R6 剩余部分 —— ✅ 已解决（Session 258）**：
>    守方 Army 进入郡域场景后的揭示归属（多线 AI 行为前置）——`computeRevealedNodeIds`
>    揭示源新增第 5 条「守方 Army 所在县」（非攻方 Army 所在节点，含一跳邻接）；
>    mask 投影保留其 `armyIds`（玩家可见驻军），`deployments` 快照仍只保留攻方条目
>    （部署历史不泄露）。f9 真实流程 + fog 单测 10/10 验证。
| **R7 ✅** | S09 | 宫廷人脉语义与字段迁移 | 不再由成年女性数量换算；历史女角边界不变 |
| **R8 ✅** | 跨系统 | 成长入口收敛和 24 回合情景平衡 | 3 城/10 将/2 场战争测试中每回合有明确取舍（54/54） |

## 四、当前状态

- 本文与相关设计摘要先行生效，作为后续实现的目标基线。
- R1 已完成：运行时统一为月推进，并提供季度/年度节拍；这不等于 R5 的季度内政与年度预算已实装。
- R2 已完成：登用与结盟共享百分点纯函数；结盟接权威 RNG，日志/UI 同源。声望、戒备和利益冲突
  字段尚未实装，当前按 0，仍属外交 Demo 而非完整 16 行动。
- R3 已于 Session 244 完成：四种单挑倾向与吕布公平性通过引擎、存档、API、UI、
  确定性及浏览器验收；下一次只处理 R4，不并行启动 R5。
- R4 已于 Session 245 完成：三入口统一为 `auto / standard / tactical`，同一交战快照
  只能选定一种模式；自动由服务端一次推演，标准逐回合，六角映射既有 BattleState，
  三者最终均以 `settlementApplied` 幂等回写 CampaignArmy。
- R5 已于 Session 246 完成：三类持续项目、暂停损失、年度预算与递增行政成本已实装。
- R6 已于 Session 247 完成最低验收：每势力最多双线，前线动态留守；停战、两月粮不足或
  兵力低于守军 55% 时撤回。未修改武将五维，军事流固定 seed 复现 38/38。县级主动 AI、
  守方 Army 入郡域场景仍按上文明确留 R6 后续/BF-P5；**Army—县位置映射、真实路径补给
  与郡域迷雾已分别由 BF-P5 Session 254 / Session 256 解决（见上文第 1、2 条）**；
  **守方 Army 入郡域场景与县级主动 AI 已于 Session 258 / Session 259 完成**（见下文）。
- R7 已于 Session 248 完成：新局、运行时、Schema 与新存档只使用
  `courtNetwork/courtNetworkOpportunities`；开局机会由商业/民心/首都地位派生，结交成功率
  不读取人口。旧 v1 `beauty*` 加载后幂等迁移并删除旧键，历史女角来源和家族边界不变。
- R5 已于 Session 246 完成：农业/商业/城防成为9/6/12月持续项目，总成本300/400/500金；
  启动首付1/3，余款逐月扣除，武将不可用或资源不足即暂停，第三个暂停月起损失进度。
  12月预算列明金粮收入、民军粮耗、项目成本和递增行政成本；未实装俸禄与基线战争损失
  明确列0。1/3/10城专项验收17/17，无即时免费增益。
- R8 已于 Session 249 完成：现有运行时字段只通过“人物成长、军团战备、阵型精通、
  战役态势”四个入口解释；未新增升级字段或按钮。固定 3 城/10 将夹具真实推进 24 个月，
  调用持续开发、征兵、训练、赈济、回合结算与两次权威自动战斗，每月均记录所选和至少
  一项放弃项，专项 54/54。属性/技能自动升级、功绩等级和阵型双轴成长仍按原文后置。
- BF-P1 可继续作为独立战场的工程主线，但新增玩法内容应让位于上述一致性修复。
