# 架构硬化审计：0-B 前质量门禁

> 状态：证据审计与候选路线，不代表所有重构已批准或已实现。审计基于 Session 132 后代码；执行时仍须一次主攻一个 Sxx 系统，并先测量再改造。

## 一、当前事实

| 议题 | 仓库证据 | 结论 |
|---|---|---|
| 服务端权威状态 | `server/src/services/game.ts` 持有当前 Game 和串行写锁；三级战斗状态均在 GameState 内 | 单一写入口成立，但编排器体积仍值得继续审计 |
| WebSocket | `server/src/ws/broadcast.ts` 只发送回合进度、事件提示和 hello | 当前没有“WS 全量 GameState 导致卡顿”的证据；增量状态协议不是立即任务 |
| 客户端 Store | `client/src/stores/gameStore.ts` 单一 Zustand store，整体 `game` 投影与 UI 状态共存 | D-0B-1 的 slice + 细粒度 selector 方向成立；必须保持一个服务端投影真源 |
| 静态数据校验 | `server/src/data/loader.ts` 在启动和城市文件 mtime 变化时用 Zod 校验整包 | 不是每回合重复 parse；不可仅凭规模猜测删除运行时边界校验 |
| 随机数 | **S10 六角战斗已收口**：基础伤害、火计、战法、暴击链和敌军六角行动均显式注入权威 `runtimeRandom`；单挑及其他域仍待处理 | `verify-battle-rng` 5/5 证明读档后伤害与事件链一致；继续按模块收口，不一次重写全部引擎 |
| 存档 | `shared/types/save.ts` 的 `SaveSlot.snapshot` 直接引用 `GameState`，S16 尚未实现 | 首次存档实现前需要独立版本信封、迁移链和加载校验；不应直接冻结当前 GameState 为永久格式 |
| 引擎验证 | shared 105 测试；战役 62 项、S16 实体 10 项、战役域 9 项、三级战斗边界 24 项、外交域 11 项、谍报域 12 项、计谋域 9 项及完整快照 10 项检查均已接入默认 CI；server 仍有多个独立 `verify-*.ts` | Gate 1 已有八个稳定集成检查；其余脚本仍应逐个评估确定性与端口依赖后再接入 |
| 战斗持久化边界 | 六角、战场地图、白刃战已收口到 `GameState.activeBattles[0]` / `activeBattlefield` / `activeMelee` | 三级战斗运行时已消除模块级双真源；实际恢复仍需完整快照、迁移与生产读档 |

## 二、建议顺序

### Gate 1：恢复绿色回归基线

- ✅ 已明确设施建造回合化的正式预期，修正旧断言并补充扣金、驻守锁、进度和日志验证；战役检查 62/62。
- ✅ 已增加根级 `pnpm verify-campaign`，并接入默认 CI。
- 把现有验证脚本分成：确定性单元测试、引擎集成测试、需要真实服务/UI的冒烟测试。
- CI 后续继续逐个纳入稳定、无端口依赖、可确定复现的引擎测试；不要为了统一而一次性重写所有脚本。

### Gate 2：存档版本与随机性契约

存档建议使用独立信封，而不是直接在运行时 `GameState` 根部承担所有持久化职责：

```typescript
interface SaveEnvelopeV1 {
  schemaVersion: 1;
  createdAt: string;
  updatedAt: string;
  scenarioId: number;
  snapshot: GameStateV1;
}
```

- 加载路径必须是：解析 JSON → 按版本迁移 → 当前版本 Zod 校验 → 恢复运行时上下文。
- `isProcessing`、连接、网络重试、动画、选择框等瞬态状态不得进入存档。
- ✅ 随机性目标已拍板为“确定性续玩”：保存可序列化 PRNG 当前状态，不做完整命令回放；读档不得改变同一后续指令的随机因果链。

进度（Session 148）：v1 加载/内存恢复边界已经完成，并按用户拍板采用“确定性续玩”。信封现要求 `xorshift32-v1` 的当前内部 state + draws；`SerializableRng` 和服务端单一权威实例可序列化、恢复并审计消费次数。共享测试 112/112；真实进行中战斗恢复后连续 8 次随机序列及 draws 完全一致，迁移/恢复 19/19。此为基础设施切片：现存各域直接 `Math.random()` 尚需逐域替换或注入，在完成前不宣称整局确定性；当前亦无磁盘/API/UI 存取，Gate 2 未结束。由于尚无已发布生产 v1 存档，本次在 v1 开发期内补齐必填 `rng`，未虚构旧格式迁移。

模块进度（Session 149）：第一模块 S10 六角战斗完成。`battle/damage.ts`、`battle/simpleAi.ts`、`engine/battle.ts` 已无直接 `Math.random()`；服务层对玩家攻击、火计、战法和敌方阶段显式传入 `runtimeRandom`。全局直接调用文件由 15 降至 12；`aiMilitary.ts` 仅因复用伤害函数接入随机源，其目标选择等 S15 行为尚未收口。未发现第三方库内部随机调用需要强改。

### Gate 3：编排器与 Campaign 的职责审计

不预先决定采用 Command Bus、类式 Domain Service、Immer 或 StateMutation。先为 `game.ts` 每个导出操作建立调用图，再按以下信号拆分：

- 相同锁、权限、脱敏和广播样板重复出现；
- 一个函数同时做输入校验、随机解析、规则计算、跨系统结算和传输组装；
- 测试某条规则必须构造无关的全局运行时状态；
- 模块存在循环依赖或无法单独运行。

优先提取窄的命令处理函数和纯规则；权威状态提交仍由一个事务边界完成。事件/Mutation 日志只有在回放、审计、增量同步或跨系统订阅有明确消费者时才引入，避免为模式而模式。

Campaign 拆分候选：

1. `campaign-rules`：战力、补给、路径、围城概率等只读计算。
2. `campaign-resolution`：接受显式 RNG，产出结构化结果和 roll 记录。
3. `campaign-apply`：在单一事务内应用伤亡、归属、武将状态与战后结算。

是否返回完整不可变 `GameState`、局部 patch 或受控 draft，要通过性能与可测试性 spike 决定，不能在设计阶段硬选。

### Gate 4：0-B 性能基线

- 使用符合当前 Zod Schema 的合成数据或扩容夹具，不把不合法“假 GameState”作为结论依据。
- 至少测量：静态数据加载/校验、单回合结算、玩家状态裁剪+JSON序列化、关键路径规划、地图首次渲染/缩放/选择和客户端提交次数。
- 固定环境、数据规模、预热次数、重复次数与统计量（中位数、p95）；保存基线 JSON/Markdown，CI 先观察趋势，不立即用任意 50ms 阈值阻断。
- Profile 确认热点后再选择索引、memo、viewport culling、缓存或 Worker。105城不自动意味着需要空间索引；城市官道图与人物归属更可能适合 ID 索引/邻接表。

## 三、不直接采纳的建议

- **全套 Command Bus + StateMutation 一步到位**：尚无证据证明收益大于迁移成本。
- **所有系统通过事件总线互相通信**：会模糊服务端权威调用链，且事件顺序/事务失败更难推理。
- **整份 GameState 快照测试作为主要集成策略**：快照过大、随机字段和无关排序会制造脆弱更新；优先断言领域不变量和关键结果，必要时对规范化子集做快照。
- **运行时不再 Zod 校验**：外部 JSON/存档仍是不可信边界；是否优化应先基准测量。
- **Canvas 动画一律用 ref 绕过 React**：只适用于高频表现性状态；权威位置和可交互终态仍须与声明式状态收敛，避免双真源。
- **宿主字体 fallback**：违反 FontBarrier 与工程字体资产闭环，明确禁止。

## 四、UI 与规则可见度

复杂系统需要解释性 UI，但不应把所有公式裸露给玩家。每个新规则至少提供：动作成本、关键正负修正、结算结果、失败原因和可追踪战报。百分比预测只有在数据确实可见且算法可稳定解释时展示；受迷雾影响的内容应给区间或“不明”，不能泄露服务端隐藏状态。

程序化头像继续使用参数化 SVG/CSS 与 `avatarGene`；`feTurbulence` 已在当前四人切片中使用。卷轴/印章动画属于 P5 体验增强，必须遵守减少动态效果偏好和性能预算，不能先于完整头像数据层。
