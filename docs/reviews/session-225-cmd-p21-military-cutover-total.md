# Session 225 — CMD-P21 军事原子切换总验收

## 结论

军事命令域迁移完成。底部命令坞“军事”是现有玩家军备、Campaign 编成与 Campaign 军令
的唯一写入口，固定为 `军备｜编成｜军令｜战报` 四分面。右栏仅保留城市只读提示，
左栏“战役”仅保留军团详情与战报读取；旧写控件均已物理删除，不是 CSS 隐藏。

`/api/game/march` 与 `marchOnCity` 仅为 S21 现有郡域接战兼容适配保留，没有玩家 UI 入口。
本轮未改变 API、引擎、数值、RNG、Schema 或存档。

## 原子验收

新增 `pnpm verify-cmd-p21-headless`，在 1440×900 的真实浏览器中完成：

- 展开左右旧面板，确认 `btn-march`、`btn-conscript`、`btn-train` 及左栏旧编成/军令写按钮为 0；
- 确认军事抽屉与四分面各自唯一；
- 军备：征兵取消不改权威状态，确认后洛阳得兵 437；
- 编成：取消不改权威状态，确认后创建带参谋的“曹仁军”；
- 军令：激励取消不改权威状态，确认后士气 +15；
- 战报：同一抽屉汇总本轮征兵与出征日志；
- 控制台错误 0。

## 回归

- shared：19 文件 198 项通过；
- client：9 文件 25 项通过；
- Campaign：71/71；
- civil RNG：12/12；
- typecheck、lint、validate-data、build、diff-check 全绿；
- build 仅保留既有 `>500 kB` chunk warning。

## 后续边界

CMD-P16～P21 至此完成军事域迁移。下一会话仍留在 S20 时，应先审计尚未迁移的
内政、计略、情报、家族与屯田域并排定单域顺序；不得在一次会话并行启动多个领域。
