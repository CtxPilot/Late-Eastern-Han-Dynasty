# Session 217：CMD-P13 外交交涉迁移

> 范围：S20 / S08。命令坞外交“交涉”分面接入进贡与献美；结盟仍留旧入口，
> 点化女间谍仍归 S07 情报。
>
> **后续状态回注（Session 218）**：CMD-P14 已启用“盟约·结盟”；旧外交仍保留，
> 下一步为原子切换及点化情报入口收口。

## 一、交付

- “交涉”分面启用，与“势力”共享同一目标选择；不复制目标缓存。
- 进贡显示己方总金、成本和按政治阶段计算的友好增量。
- 献美显示己方美女库存、转移成本、友好增量和点化额度后果；战争目标禁用并说明原因。
- 两项均复用 `tribute()` / `transferCourtNetwork()`、既有 API/引擎与唯一
  `CommandConfirmDialog`；取消保留目标/分面，成功清除终审。
- 确认前按最新 Zustand 快照复验目标存活、关系、总金与库存，服务端继续最终权威校验。

## 二、过渡边界

- CMD-P13 期间新旧进贡/献美短暂双入口，只用于同源对照；两边调用同一 action/API，
  没有第二份业务状态。
- “盟约”仍 disabled，新抽屉没有结盟提交按钮。
- “点化女间谍”只在说明中指出属于情报域，新抽屉没有点化提交按钮。
- 旧外交必须保留到后续盟约迁移完成并执行原子切换。

## 三、验证

`pnpm verify-cmd-p13-headless` 在 1440×900 英雄集结曹操局实际完成：

- 新进贡取消权威状态不变；确认友好 +15、日志 `tribute`；
- 零库存献美禁用；通过正式内政寻访建立库存并由旧 action 同步权威快照；
- 新献美取消不变；确认己方库存 −1、友好 +12、点化额度 +1、日志 `gift_beauty_dip`；
- 战争目标献美禁用；新结盟/点化写入口均为0；console error=0。

敌方美女库存受 S06 玩家状态裁剪，浏览器不越权断言；服务端双方库存转移继续由
`verify-negotiation-r2` 的确定性断言覆盖。
