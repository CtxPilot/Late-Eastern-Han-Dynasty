# Session 229 · CMD-P25 内政原子切换总验收

## 决策

用户批准推荐方案：在“内政·总览”放置明确标注为 `S09 · 宫廷人脉` 的跨系统寻访卡片，
并删除右栏旧寻访入口。四个内政分面保持不变；寻访的业务归属仍为 S09，不因 UI 容器或
历史 `/civil/seek-beauty` 路由前缀改归 S03。

## 实现

- 总览显示势力美女库存、本城可寻次数、60金成本与 S09 标识。
- 寻访使用统一 `CommandConfirmDialog`，显式提交所选己方 `cityId`。
- 确认前从最新 `GameState` 复验城市存在/归属、可寻次数≥1、金≥60；服务端继续最终兜底。
- `seekBeauty(cityId?)` 支持显式城市，同时保留无参兼容调用。
- 右栏农业、商业、城防、施米、寻访五个旧写入口全部物理删除。
- 未修改 API、S09 `beauty.ts` 引擎、成功率、消耗、RNG、Schema 或存档。

## 验收

`pnpm verify-cmd-p25-headless` 在 1440×900 下创建曹操局并实际操作洛阳：

- 右栏五个旧写按钮为0，S09 跨系统卡片唯一；
- 寻访取消后权威状态不变；确认后扣金60，本次成功，库存+1、可寻次数−1；
- 农业取消后状态不变；确认后农业+28、商业+20、城防+21、民心+9；
- 对应行动日志齐全，浏览器控制台错误0。

回归同时通过：typecheck、lint、shared 198/198、client 28/28、validate-data、生产构建与
`git diff --check`。构建仅保留既有大 chunk warning。
