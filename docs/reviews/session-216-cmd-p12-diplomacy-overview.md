# Session 216：CMD-P12 外交只读势力与关系摘要

> 范围：S20 / S08。命令坞外交抽屉仅接入只读浏览，旧左栏外交继续是唯一写入口。
>
> **后续状态回注（Session 217）**：CMD-P13 已启用“交涉”分面的进贡与献美；
> 结盟仍留旧入口，点化仍归情报。本文的“写按钮为0”仅是 Session 216 的迁移中基线。

## 一、交付

- 新增 `DiplomacyOverviewDrawer`，固定展示 `势力｜交涉｜盟约` 三分面；本轮仅“势力”可用。
- 存活的非玩家势力按 id 稳定排列，支持单选目标。
- 摘要从当前已裁剪 `GameState` 派生关系、友好、君主、都城、领土与已知总兵力。
- `交涉`、`盟约`禁用并说明仍用旧面板；抽屉内没有进贡、献美、结盟或点化提交按钮。
- 旧外交手风琴、store action、API、引擎、概率、成本及存档均未改变。

## 二、权威与草稿边界

- 权威：`game.factions/officers/cities/diplomacy`。
- 派生：关系标签、目标资源汇总及摘要排列。
- 客户端意图：仅 `selectedFactionId`；目标灭亡或剧本切换后回退首个存活目标。
- 无业务草稿、无终审、无 API 调用、无第二份关系缓存。

## 三、验证

- 客户端 8 文件 21 项测试通过；新增纯模型测试覆盖排除玩家/灭亡势力、关系字段及城兵汇总。
- `pnpm verify-cmd-p12-headless` 在 1440×900 英雄集结曹操局遍历 3 个目标，逐项确认摘要字段，
  断言新抽屉写按钮为 0；再从旧入口实际进贡，新抽屉重开后友好即时 +15，console error=0。
- typecheck、lint、diff-check 通过。

## 四、后续

CMD-P13 迁移“交涉”分面，仅承接进贡与献美。必须复用既有 store action/API 和统一终审；
点化保持 S07 跨域边界，结盟仍留旧入口至后续“盟约”分面。
