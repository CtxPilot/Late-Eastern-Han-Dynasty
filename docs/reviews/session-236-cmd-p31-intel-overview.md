# Session 236 · CMD-P31 情报四分面只读态势

## 范围与结论

- 继续主攻 S20，仅新增命令坞情报只读抽屉。
- 固定 `态势｜人员｜任务｜反间` 四分面；派生密探容量/状态、可见城情报、己方任务、
  己方反间、己方扣押敌谍与点化额度。
- 数据只取玩家收到的迷雾裁剪 `GameState`，模型再次过滤己方人员、任务与城市归属；
  不展示敌方在役密探或敌方反间。
- `intel/recon` 导航意图落到“任务”分面并显示迁移期说明。
- 本轮不新增提交按钮；旧 `SpyPanel` 继续作为 S07 六类写链唯一入口。

## 验证

- 模型单测：敌方在役密探、敌方任务与敌方反间不会进入摘要。
- 1440×900 Headless：四分面各 1、新写入口 0；旧 `SpyPanel` 与招募/训练/点化/派遣
  各 1。通过旧入口确认招募 1 名密探后，新“人员”摘要即时同步；console error=0。
- shared 198/198、client 32/32；client typecheck、全仓 lint、validate-data、build、
  diff-check 通过（build 仅既有大 chunk warning）。

## 后续

CMD-P32 迁移普通招募、女间谍训练与献美点化三条人员建设写链；任务、反间、俘虏处置
继续留到 P33。
