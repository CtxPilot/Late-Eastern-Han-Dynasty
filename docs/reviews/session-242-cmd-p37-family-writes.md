# Session 242 · CMD-P37 家族写链迁移验收

## 结论

- 婚配与手动跟随检查已迁入命令坞 `FamilyOverviewDrawer`，旧 `FamilyPanel` 写入口归零。
- 两条命令均复用 `CommandConfirmDialog`；取消保留婚配草稿且不改变权威状态。
- 手动跟随终审明确披露会消费权威 RNG、结果可能无人投奔；确认前复验仍有在野武将。
- 用户批准保留 `giftedToOfficerId` 随侍随迁。引擎统一改称“家眷/随侍”，行为、字段、
  API、数值、RNG 算法与存档契约不变。

## 自动与浏览器验证

- `verify-family-rng`：36/36；新增随侍加入势力、迁城、随武将流落、关系保持四项。
- client：36/36；覆盖婚配最新状态复验、随侍他人门禁与无在野候选门禁。
- `verify-cmd-p37-headless`（1440×900）：旧婚配/跟随入口 0/0，新入口 1/1；
  婚配取消状态不变且保留草稿，确认蔡琰×夏侯惇建立双向关系；跟随取消状态不变，
  确认完成；console error=0。

## 边界

- P37 不删除旧 `FamilyPanel` 只读壳；物理删除与跨 S11/年度共享结算总验收留 P38。
- 不新增父辈族谱、随机出生、纳妾/离婚，也不引入 S09 美女库存。
