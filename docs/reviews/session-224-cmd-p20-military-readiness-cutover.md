# Session 224 · CMD-P20 军备迁移审查

## 结论

现有正式军备写链只有征兵与训练，现均已迁入 `军事 → 军备`，并显式选择己方城市。
右栏“军事操作”删除两枚旧写按钮，只保留导航说明与取消选中，因此玩家军备入口唯一。
募兵、补充、整编、驻防、伤兵、复员尚无权威规则，本轮未制作假按钮。

## 权威与终审

- `conscript(cityId?)`、`trainTroops(cityId?)` 允许命令台显式传城；未传参时仍兼容旧 store 调用。
- 征兵终审展示 80 金、120 粮、成年男丁消耗，提交前复验城市归属、资源与可征人口。
- 训练终审展示 60 粮，提交前复验归属、粮食与至少 100 驻军。
- 权威随机增量、人口同步、资源公式与日志继续由既有 S03 引擎负责；无 API、Schema、数值或存档变化。

## 验证

`verify-cmd-p20-headless` 在 1440×900 实际验证洛阳：

- 右栏旧征兵/训练按钮为 0；
- 取消征兵后权威状态不变；
- 确认征兵获得 437 兵、成年男丁同步减少 437，扣 80 金/120 粮；
- 确认训练士气 +9、扣 60 粮；
- 征兵/训练日志齐全，console error=0。

## 后续

CMD-P21 做军事迁移总验收：四分面、旧写 DOM=0、唯一入口计数、编成→军令→军备→战报
完整链与历史 Headless 边界。`/march` 兼容仍留 S21。
