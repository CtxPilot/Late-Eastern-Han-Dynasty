# Session 238 — CMD-P33 情报任务/反间/俘虏迁移

## 结论

S07 五类任务派遣、反间驻防/撤防与俘虏处决/释放已迁入命令坞
`IntelOverviewDrawer`。旧 `SpyPanel` 只剩只读名册，按钮与选择器均归零；其物理删除及
情报域原子总验收留 CMD-P34。

## 实现

- “任务”分面显式选择空闲密探、任务类型与敌方城池。
- 客户端可见前置覆盖：人员归属/状态/冷却、女间谍专属、非探秘盟友门禁、官道邻接及
  邻接己方城单城支付能力；确认前按最新 `GameState` 复验。
- “反间”分面显式选择己方城和空闲密探；驻防、撤防均进入统一终审。
- 己方扣押敌谍卡片提供处决/释放，并在终审前复验俘虏状态与扣押势力。
- 复用现有 store、API 与服务端 S07 权威引擎；未改规则、数值、RNG、Schema 或存档。

## 验证

- `verify-cmd-p33-headless`：1440×900 实际点击驻防、撤防和探秘任务取消/确认；确认后
  生成下邳 detailed 详报。旧面板按钮/选择器/任务入口均为0，新任务/驻防/撤防各唯一，
  俘虏空态正确，console error=0。
- 俘虏有数据时的操作前置由客户端模型测试覆盖；浏览器验收不增设制造俘虏的测试后门。
- shared 198/198、client 34/34、plot/spy 34/34；lint、validate-data、build、
  diff-check 全绿。构建仅有既有大 chunk warning。

## Next

CMD-P34：物理删除旧 `SpyPanel`，复验计略→`intel/recon` 落点，并做情报全链唯一入口
与原子切换总验收。
