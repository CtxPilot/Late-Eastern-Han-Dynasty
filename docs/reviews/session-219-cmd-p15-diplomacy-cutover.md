# Session 219 — CMD-P15 外交原子切换

## 结论

CMD-P11～P14 已覆盖的外交读写能力现正式切换为命令坞唯一入口。旧 `LeftPanel`
外交手风琴、势力卡片及进贡/献美/点化/结盟按钮均已物理删除，不以 CSS 隐藏。

旧外交卡片中唯一属于 S07 的“点化女间谍”迁入 `SpyPanel`“策反点化”区：

- 仅列出存活且拥有 `plantableBeauty` 额度的目标；
- 送交统一谍报终审，取消不提交；
- 确认前复验目标存活、点化额度和己方总金；
- 敌方美女库存受 S06 迷雾裁剪，客户端不得用裁剪值阻断；服务端仍权威校验库存。

没有新增 API、Schema、规则、概率、成本或测试专用业务写接口。

## 浏览器终审

`CDP_PORT=9238 pnpm verify-cmd-p15-headless` 在 1440×900 下实际完成：

1. 正式寻访取得美女库存；
2. 命令坞外交献美，点化额度增加 1；
3. 打开左栏谍报，点化先取消，权威状态不变；
4. 再次确认，额度减少 1，生成一名 `agentKind='female'` 的己方密探；
5. 旧外交 DOM=0，新外交献美入口=1，情报点化入口=1；
6. 首条日志为 `spy_plant_female`，浏览器控制台错误=0。

## 历史基线说明

CMD-P11/P12 的脚本用于记录原子切换前旧入口基线，P15 后不再作为现态回归执行。
CMD-P15 脚本接管“旧 DOM 必须为 0”和跨域完整链路断言。
